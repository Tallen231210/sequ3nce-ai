# Production Environment Configuration
# These values are used when building the production app

# Web App URL (production)
WEB_APP_URL=https://sequ3nce.ai

# Audio Service URL (production WebSocket endpoint)
AUDIO_SERVICE_URL=wss://amusing-charm-production.up.railway.app

# Note: Clerk keys are already embedded in the app for production
# The app uses the test keys by default, replace with live keys when ready
