
.env.local
.vercel
.env*.local

# Playwright
tests/.auth/
test-results/
playwright-report/

# clerk configuration (can include secrets)
/.clerk/

# Playwright
test-results/
playwright-report/
blob-report/

# Sentry Config File
.env.sentry-build-plugin

# Backups of env files hold live secrets. `.env*.local` does NOT match a
# name ending in something else, which is how a production key once reached
# a commit. Match the prefix instead.
.env.local.*
*.prod-backup
